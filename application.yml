server:
  port: 8888
spring:
  application:
    name: @project.artifactId@
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/aaliagab/WasteManagementConfigurations/"
          default-label: main
          clone-on-start: true
